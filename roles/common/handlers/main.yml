---
# Handlers for docker and kubelet

- name: restart docker
  systemd:
    name: docker
    daemon_reload: yes
    state: restarted

- name: Restart kubelet
  systemd:
    name: kubelet
    daemon_reload: yes
    state: restarted